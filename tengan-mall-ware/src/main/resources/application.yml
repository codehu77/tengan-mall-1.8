spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/tengan_mall_wms
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: tengan-mall-ware

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 11000